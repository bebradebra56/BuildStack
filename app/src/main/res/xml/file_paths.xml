<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internal app-specific storage (no external storage permissions needed) -->
    <files-path
        name="build_stack_photo_path"
        path="."/>
    <cache-path
        name="internal_cache"
        path="." />

</paths>

